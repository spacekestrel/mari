{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "Mari",
  "version": "0.2.0",
  "identifier": "com.mari.editor",
  "build": {
    "beforeDevCommand": "npm run dev",
    "devUrl": "http://localhost:1420",
    "beforeBuildCommand": "npm run build",
    "frontendDist": "../build"
  },
  "_securityNote": "The Content Security Policy lives in svelte.config.js, not here. It needs the hashes of the app's own inline scripts, which SvelteKit recomputes on every build; hashes written here by hand would go stale and the app would refuse to start. Setting app.security.csp as well would apply both policies at once, and the strictest wins, which would block the very scripts the hashes allow.",
  "app": {
    "windows": [
      {
        "title": "Mari",
        "width": 800,
        "height": 600,
        "maximized": true,
        "decorations": false
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ]
  }
}
