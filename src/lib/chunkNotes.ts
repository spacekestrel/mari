/**
 * Per-passage notes. They live inside the `.mari` file (see mariBundle); this
 * module only holds what's shared about them.
 */

/** Long enough for a reminder, short enough that it stays a note. */
export const CHUNK_NOTE_MAX_LENGTH = 280;
